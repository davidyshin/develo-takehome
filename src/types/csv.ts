// I would love to name this file a better name, but I can't think of a better name.
// hmm.

export interface LMSRow {
  Sex: string; // 1 for male, 2 for female
  Agemos: string;
  L: number;
  M: number;
  S: number;
}
